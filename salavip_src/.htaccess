RewriteEngine On
RewriteBase /salavip/

# Block direct access to includes
RewriteRule ^includes/ - [F,L]

# Allow image files in uploads, block everything else
RewriteRule ^uploads/.*\.php$ - [F,L]
RewriteRule ^uploads/.*\.(jpg|jpeg|png|webp|gif)$ - [L]
